---
'start-command': minor
---

Add SSH isolation support for remote command execution. Implements SSH backend for executing commands on remote servers via SSH, similar to screen/tmux/docker isolation.
