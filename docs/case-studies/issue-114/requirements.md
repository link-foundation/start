# Requirements extracted from Issue #114

| #   | Requirement                                                                                                                               | Satisfied by                                                                                                                                                        |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| R1  | Fix all CI/CD issues so the new package version can publish.                                                                              | Rust release mode support in `scripts/version-and-commit.mjs`, changelog-aware GitHub Release creation, badge generation, and Windows timeout fixes.                |
| R2  | JavaScript releases must use language-prefixed tags and titles.                                                                           | Existing `releaseTag` / `releaseName` helpers remain wired through `js.yml` with `--prefix "js-"`; new tests keep this behavior covered.                            |
| R3  | Rust releases must use language-prefixed tags and titles.                                                                                 | Existing `releaseTag` / `releaseName` helpers remain wired through `rust.yml` with `--prefix "rust-"`; Rust release creation now reaches that step.                 |
| R4  | Both JavaScript and Rust GitHub Releases must have badges for specific versions.                                                          | `packageVersionBadge` creates exact npm/crates badges after stripping `js-v` / `rust-v`; JS formatter uses it for npm, Rust release creation uses it for crates.io. |
| R5  | Compare workflows and CI/CD scripts with `js-ai-driven-development-pipeline-template` and `rust-ai-driven-development-pipeline-template`. | `templates/` contains reviewed workflow snapshots and the reviewed file list; [root-cause.md](root-cause.md) records the comparison.                                |
| R6  | Download all logs and data related to the issue into `docs/case-studies/issue-114`.                                                       | `ci-logs/`, `issue-data.json`, `issue-comments.json`, and both CI run JSON files are stored in this folder.                                                         |
| R7  | Compile a case study with root causes, timeline, requirements, online research, and possible solutions.                                   | `README.md`, `timeline.md`, this file, `root-cause.md`, `online-research.md`, and `solutions.md`.                                                                   |
| R8  | If the same issue exists in templates, report it there.                                                                                   | No matching template issue was found. The templates either already have the relevant helper coverage or use Rust-native release scripts.                            |

## Derived requirements

| #   | Requirement                                                                            | Why it is needed                                                                                                                                      |
| --- | -------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| D1  | `create-github-release.mjs` must read package-specific changelogs.                     | The JS changelog is `js/CHANGELOG.md`; the Rust changelog should be `rust/CHANGELOG.md`. Reading root `CHANGELOG.md` produced fallback release notes. |
| D2  | Changelog extraction must support both heading formats in this repo.                   | JS uses Changesets headings like `## 0.26.0`; Rust uses Keep a Changelog headings like `## [0.14.1] - 2026-05-02`.                                    |
| D3  | Release tests must cover prefixed tags, badge normalization, and changelog extraction. | These are the regressions that made the prior prefix work incomplete.                                                                                 |
| D4  | Windows CI tests must set test-level timeouts that match helper-level timeouts.        | The failed tests had helper timeouts of 10-30 seconds but default test timeouts of 5 seconds.                                                         |
