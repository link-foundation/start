# zizmor configuration — https://docs.zizmor.sh/configuration/
rules:
  unpinned-uses:
    config:
      policies:
        # These publishers are trusted at tag granularity: their release tags
        # are the reference a reader is meant to recognise at a glance, and
        # they are the same publishers the CI/CD templates trust.
        # Everything else must be pinned to a full commit hash.
        actions/*: ref-pin
        github/*: ref-pin
        docker/*: ref-pin
        astral-sh/*: ref-pin
        lycheeverse/*: ref-pin
        zizmorcore/*: ref-pin
        changesets/*: ref-pin
        '*': hash-pin
